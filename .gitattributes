# Ensure all shell scripts use LF
*.sh text eol=lf

# Ensure Dockerfiles use LF
Dockerfile text eol=lf

# Ensure Python files are normalized
*.py text eol=lf

# Prevent CRLF anywhere in the repo
* text=auto eol=lf
